import {NextResponse} from "next/server";import {prisma} from "@/lib/db";import {requireAdmin} from "@/app/admin/_lib/session";import {toJsonSafe} from "../_lib/response";export const dynamic="force-dynamic";
export async function GET(){if(!await requireAdmin())return NextResponse.json({success:false,error:"Unauthorized"},{status:401});try{const data=await prisma.$queryRawUnsafe("SELECT c.*, COUNT(p.id) post_count FROM forum_categories c LEFT JOIN forum_posts p ON p.category_id=c.id GROUP BY c.id ORDER BY c.sort_order,c.name");return NextResponse.json(toJsonSafe({success:true,data}))}catch{return NextResponse.json({success:false,error:"Failed to load categories"},{status:500})}}
export async function POST(request:Request){if(!await requireAdmin())return NextResponse.json({success:false,error:"Unauthorized"},{status:401});const b=await request.json().catch(()=>({})),name=String(b.name||"").trim(),description=String(b.description||"").trim();if(!name||!description)return NextResponse.json({success:false,error:"Name and description are required"},{status:400});try{await prisma.$executeRawUnsafe("INSERT INTO forum_categories (name,description,icon,color,sort_order,is_active,created_at,updated_at) VALUES (?,?,?,?,?,1,NOW(),NOW())",name,description,String(b.icon||"fas fa-comments"),String(b.color||"#CE6723"),Number(b.sort_order||0));return NextResponse.json({success:true},{status:201})}catch{return NextResponse.json({success:false,error:"Failed to create category"},{status:500})}}
export async function PATCH(request:Request){if(!await requireAdmin())return NextResponse.json({success:false,error:"Unauthorized"},{status:401});const id=new URL(request.url).searchParams.get("id"),b=await request.json().catch(()=>({}));if(!id)return NextResponse.json({success:false,error:"Category id is required"},{status:400});try{await prisma.$executeRawUnsafe("UPDATE forum_categories SET name=?,description=?,icon=?,color=?,sort_order=?,is_active=?,updated_at=NOW() WHERE id=?",String(b.name||"").trim(),String(b.description||"").trim(),String(b.icon||"fas fa-comments"),String(b.color||"#CE6723"),Number(b.sort_order||0),b.is_active?1:0,id);return NextResponse.json({success:true})}catch{return NextResponse.json({success:false,error:"Failed to update category"},{status:500})}}
export async function DELETE(request:Request){if(!await requireAdmin())return NextResponse.json({success:false,error:"Unauthorized"},{status:401});const id=new URL(request.url).searchParams.get("id");if(!id)return NextResponse.json({success:false,error:"Category id is required"},{status:400});try{const rows=await prisma.$queryRawUnsafe("SELECT COUNT(*) count FROM forum_posts WHERE category_id=?",id) as Array<{count:number|bigint}>;if(Number(rows[0]?.count||0)>0)return NextResponse.json({success:false,error:"Cannot delete a category with existing posts"},{status:409});await prisma.$executeRawUnsafe("DELETE FROM forum_categories WHERE id=?",id);return NextResponse.json({success:true})}catch{return NextResponse.json({success:false,error:"Failed to delete category"},{status:500})}}
